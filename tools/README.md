# Tooling

This directory stores infra-as-code, automation scripts, and deployment utilities.

- `precommit/`: configuration assets shared across contributors (hook mirrors, formatting rules).
- `docker/`: Dockerfiles and compose stacks for local development.
- `ci/`: reusable scripts consumed by CI/CD pipelines.
